# .streamlit/secrets.toml
# Copy this file as secrets.toml and fill in your PostgreSQL credentials.
# For demo mode (no database), simply omit this file entirely.

[database]
url = "postgresql://user:password@host:5432/techstore"
